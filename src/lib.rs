extern crate libc;
extern crate winapi;
extern crate kernel32;
extern crate user32;

use winapi::*;
use kernel32::*;
use user32::*;
use std::ptr;
use std::ffi::OsStr;
use std::os::windows::ffi::OsStrExt;
use std::mem;



#[test]
fn it_works() {
}
